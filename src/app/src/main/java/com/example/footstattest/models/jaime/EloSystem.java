package com.example.footstattest.models.jaime;

public class EloSystem {
    /*
    Algorithm Point System

     W is the result of the game (1 for a win, 0.5 for a draw, and 0 for a loss).
     This also holds when a game is won or lost in extra time.
     If the match is decided on penalties, however, the result of the game is considered a draw (W = 0.5).

    Goal difference = goal difference / matches played
     */

    /*
    for team in standings:
        calculate team.elo
        add_to_rankings(team, elo)

    get_elo(){
        points = (wins * 1) - (draw * 0.5) + (loss * 0)
        points += goal difference / matches played
    }

    add_to_rankings(team, elo){
        for current in rankings:
            if team.elo < current:
                continue
            else:



    }


     */

}
